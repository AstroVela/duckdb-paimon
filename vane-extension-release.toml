# Preparation pin includes the production public key but is not a Vane release.
# Update this exact pin by reviewed PR after a non-development Vane release is on
# PyPI. Until then, the release preflight deliberately fails before signing.
schema_version = 2
name = "paimon"
extension_config = "extension_config_vane.cmake"
build_extensions = ["httpfs", "parquet"]
native_test_selection = "test/sql/paimon_scan.test"

[vane]
repository = "AstroVela/vane"
revision = "033b549afcb498633fd6669b26c054c00363004e"

[vcpkg]
repository = "microsoft/vcpkg"
revision = "84bab45d415d22042bd0b9081aea57f362da3f35"
