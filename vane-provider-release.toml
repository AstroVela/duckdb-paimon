interpreters = ["cp310", "cp311", "cp312", "cp313", "cp314"]
platforms = ["manylinux_2_28_x86_64"]
# TestPyPI's per-file upload limit, independent of Vane's artifact budgets.
max_wheel_bytes = 100000000

[providers.paimon]
distribution = "vane-extension-paimon"
dependencies = []
